# SPARK — Completed Tasks: April 2026

## 2026-04-15: spark ingest — LLM-ready context generation via repomix

### spark ingest — repomix integration
- [x] `ingest_path()` mirrors repos_root structure in `~/.config/spark/ingest/`
- [x] `generate_ingest()` runs `npx repomix@latest --style markdown --output <path>`
- [x] `spark ingest <query>` — generates LLM-ready context for a single repo
- [x] `spark ingest --all` — batch generation for all managed repos
- [x] `spark ingest --compress` — tree-sitter compression (~70% token reduction)
- [x] `spark ingest` (default) — lists all existing ingests with fresh/stale status
- [x] Column alignment dynamic based on longest repo name
- [x] Base path shown once, relative `owner/name.md` per repo in list output
- [x] `--read` filters out base64/binary lines (SVG data, image blobs) before printing
- [x] Replaced `is_repomix_available()` with `is_npx_available()` — fix for false "not found" on `--compress`
- [x] CLI split: `src/cli/ingest.rs` extracted from `src/cli/mod.rs`

Notes: Repomix runs via npx (no global install required). Ingest files stored under `~/.config/spark/ingest/<host>/<owner>/<name>.md`. Stale threshold mirrors repo stale config (90 days). Commits: a2f1020–569848d.

---

## 2026-04-15: spark ps, AI skill, audit deps TUI, system nav, smoke-test

### spark ps — unified process + port inspector
- [x] New `src/cli/ports.rs` — replaces `lsof` and `ps aux | grep` with one command
- [x] Default: dev servers only; `--all` splits into SYSTEM — macOS / SERVICES / APPS sections
- [x] Query mode: `spark ps <name>` searches `ps aux`, cross-references listening ports
- [x] `spark ps --kill <target>` — interactive kill by port, PID, or name
- [x] `spark ps <query> --kill` — non-interactive kill for scripts/agents (exit 0=killed, 1=not found)
- [x] `is_dev_server()` extended: Warp (`stable`), terminal apps classified as SYSTEM — APPS
- [x] `kill_process()` stderr suppressed (`Stdio::null`) — no noise on race condition kills
- [x] Self-kill guard: filters own PID from ps_list results to prevent suicide on query match
- [x] AI skill (`assets/spark.skill.md`) updated with all `spark ps` workflows

Notes: Command named `ps` with alias `ports`. `--kill` uses `num_args=0..=1, default_missing_value=""` to distinguish interactive vs non-interactive modes. Commits: ff7ef9b, b842e57.

### AI skill file — `assets/spark.skill.md`
- [x] Created `assets/spark.skill.md` with YAML frontmatter and agent trigger conditions
- [x] Agent-oriented workflows (not reference docs): audit before commit, status before deploy, port conflicts, LLM ingest
- [x] `spark init` writes to `~/.agents/skills/spark/SKILL.md`, symlinks into `~/.claude/skills/`, `~/.codex/skills/`, `~/.gemini/skills/`
- [x] Embedded via `include_str!()` in `src/cli/system.rs`

Notes: Skills directory convention: `~/.agents/skills/<name>/SKILL.md` as canonical, symlinks per agent. Commit: ff7ef9b.

### Audit TUI — dependency vulnerability findings
- [x] `AuditModel` extended: `dep_vulns: Vec<DepVulnerability>`, `dep_cursor: usize`
- [x] `AppMessage::AuditScanResult` carries dep vulns alongside secret findings
- [x] `src/app.rs`: dep scan runs in same tokio task as secret scan — async, non-blocking
- [x] `audit_view.rs`: header shows dep vuln count (orange) or "deps clean" (green)
- [x] Project list includes `[dependencies]` row; detail panel renders severity/package/fix/summary table
- [x] `SecretAuditDeps` state wired into `scanner_keys/mod.rs` dispatch

Notes: OSV.dev batch API + npm audit. Commit: ff7ef9b.

### System cleaner navigation fix
- [x] `display_order: Vec<Option<usize>>` added to `SystemCleanerModel` — None=header, Some(i)=item
- [x] `move_up()` / `move_down()` skip None entries — cursor never lands on section headers
- [x] Same pattern as port scanner's group-aware navigation

Notes: Commit: ff7ef9b.

### ESC key support
- [x] ESC mapped in `scanner_keys` — works alongside `q` for back/close in sub-views

Notes: Commit: ff7ef9b.

### smoke-test.sh — post-install validation
- [x] `scripts/smoke-test.sh` created — validates binary, basic CLI commands, config
- [x] Intended for GitHub Actions CI after release builds

Notes: Commit: ff7ef9b.

---

## 2026-04-16: spark ingest — trs as sole backend

### spark ingest — trs integration
- [x] Dropped repomix entirely — trs is the only ingest backend
- [x] `is_trs_available()` check with clear install instructions on failure
- [x] New flags forwarded to trs: `--budget`, `--changed`, `--since`, `--deps`
- [x] `--compress` maps to `trs -l aggressive` (~93% token reduction)
- [x] Stdout capture → spark path (temporary until trs ships `-o`)
- [x] TODO marker in `generate_ingest()` for one-liner swap when trs `-o` lands
- [x] List view shows trs status (green/red) in header
- [x] Skill file: trs direct usage for agents, all new flags documented
- [x] Architecture: spark passes `--output <spark_path>` to own the path; trs owns `~/.trs/ingest/` for direct agent use

Notes: trs returns path by default, `--print` for content, `-o` for custom path (pending). Swap in `src/scanner/repo_ingest.rs` when trs ships. Commit: 702ac8e.

## 2026-04-15: spark ingest — trs -o flag, --fresh support

### spark ingest: trs -o <path> integration
- [x] Updated `generate_ingest()` in `src/scanner/repo_ingest.rs` — use `trs ingest -o <output_path>` instead of stdout capture
- [x] Removed stdout-to-file write (trs writes directly via `-o`)
- [x] Added `fresh: bool` to `IngestOptions` — maps to `trs --fresh` (skip if HEAD unchanged)
- [x] Added `--fresh` flag to `spark ingest` CLI (`src/cli/mod.rs`)
- [x] `generate_one()` displays `--fresh` in active flags label

Notes: trs shipped `-o <path>` support. spark now passes it directly — no temp file, no stdout buffer. Commit: 0851b5c.

---

## 2026-04-20: Open source docs, GitHub page, root cleanup, attribution strategy

### Root cleanup
- [x] Removed 12 legacy files from root: bash scripts, stray binaries, old logs, unused configs
- [x] Updated `.gitignore`: added `*.log`, `spark` binary, `.vscode/`, `.idea/`

Notes: Branch `docs/review-and-cleanup`. Commit: 4ff72ef.

### README rewrite + Spanish translation
- [x] `README.md` rewritten in TARS style — centered header, badges, nav links, module descriptions, CLI table, keyboard controls, tech stack
- [x] `README.es.md` created — full Spanish translation with identical structure
- [x] `CONTRIBUTING.md` created — quick start, PR checklist, commit style, issue reporting, links to `docs/dev/`

Notes: Commits: 1ccda90.

### docs/dev/ — architecture and guidelines
- [x] `docs/dev/ARCHITECTURE.md` updated to v0.5.1 — accurate module tree (`scanner_keys/` split, `cert_scanner`, `repo_tags`, `repo_ingest`, `system_categories`, `common.rs`, `cli/certs.rs`, `cli/tags.rs`, `cli/ingest.rs`, `utils/fs.rs expand_tilde`), updated state machines (SystemCleanConfirmBulk, SecretAuditPathInput), test count 127
- [x] `docs/dev/DEV_GUIDELINES.md` created — error handling rules, path conventions, where new code goes, TUI state machine checklist, pure rendering rule, concurrency model, test conventions, debug logging
- [x] `CONTRIBUTING.md` slimmed — inline standards/architecture sections replaced with links to `docs/dev/`
- [x] `docs/README.md` created — navigation index for all dev docs and task tracking

Notes: Commit: 3040b94.

### GitHub page (docs/index.html)
- [x] Version badge `v0.5.1`, test count 127, tool count 55
- [x] `white-space: pre` added to `.tui-preview` and `.cli-examples` — lines were collapsing into single paragraph
- [x] Module cards: Port Scanner and Repository Scanner missing CLI tags — fixed; Tool Updater count 61→55
- [x] `spark ps` added to CLI examples section
- [x] All GitHub URLs corrected to `dPeluChe/spark`
- [x] Footer links updated: GitHub, npm, Contributing, Architecture

Notes: Commits: 3040b94, ff2e06f, 7850685.

### Iteris attribution strategy
- [x] `docs/index.html`: `<meta name="author" content="Iteris">`, JSON-LD `SoftwareApplication` schema with `author: {Iteris, iteris.tech}`, footer "Built with Rust & caffeine by Iteris"
- [x] `README.md`: "A product by Iteris · Published and maintained by @dPeluChe"
- [x] `README.es.md`: "Un producto de Iteris · Publicado y mantenido por @dPeluChe"
- [x] `npm/README.md`: footer + corrected links and counts
- [x] `npm/package.json`: `author: {Iteris, iteris.tech}`, `contributors: [{dPeluChe}]`, `version: 0.5.1`, `homepage` → landing page, `optionalDependencies` bumped to 0.5.1

Notes: Commit: 5123ae0.

### Bug fixes
- [x] `~/.zshrc`: removed stale `alias spark="/Users/peluche/.local/bin/spark"` — was overriding the correct npm-installed binary at `/opt/homebrew/bin/spark`, causing "no such file or directory" errors outside the project folder
- [x] Removed stale v0.8.0 binary at `/usr/local/bin/spark`
- [x] `CONTRIBUTING.md` line 19: broken link `docs/TASK_TODO.md` → `docs/dev/TASK_TODO.md`

Notes: Commit: 1d40b0e.

### Task hygiene
- [x] `added:` dates added to all 10 pending tasks that were missing them (baseline: 2026-04-20)
- [x] `docs/README.md` created as navigation index

Notes: Today.
