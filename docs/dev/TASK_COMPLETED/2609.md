# SPARK — Completed Tasks: September 2026

## 2026-09-15: parallel repo status + pull overhaul

### spark status / spark pull — parallel fetch, no double-fetch, classified output
- [x] `check_statuses_parallel()` — bounded pool (`STATUS_CONCURRENCY = 8`) over `std::thread::scope` + mpsc; progress `[n/total]` to stderr
- [x] `spark status` — parallel check for uncached repos; cache saved in one batch write (`save_statuses_to_cache`) instead of N file rewrites
- [x] `spark pull` — same pool; `Behind` repos merge via new `merge_ff_only()` (`git merge --ff-only @{upstream}`) reusing the status fetch — no second network fetch per repo
- [x] `git fetch --quiet --prune` — stale remote-tracking refs auto-pruned (fixes "some local refs could not be updated" errors)
- [x] Fetch failures surfaced: `UpToDate` + failed fetch now reports `Error("fetch failed: …")` instead of a stale "Up to date"
- [x] Pull summary now separates buckets: pulled / up to date / skipped (Ahead, Dirty, Diverged — diverged no longer attempts a doomed `--ff-only`) / errors
- [x] Error output condensed to the `fatal:`/`error:` line + remediation hint (`spark rm` for dead remotes, `git refs migrate --ref-format=reftable` for case-conflicts, `git remote prune origin` for stale refs)
- [x] `list_managed_repos` reads remote/branch/last-commit via git2 — drops 3 `git` subprocess spawns per repo
- [x] `is_cache_valid` uses `saturating_sub` (no underflow on clock skew)
- [x] TUI `start_check_repo_statuses` — same bounded-pool pattern over tokio `spawn_blocking` workers; rows still update incrementally
- [x] Shared `select_repos()` tag/query filter for `status`/`pull` (was duplicated)

Notes: 145 repos checked in ~21s fresh (was sequential: minutes). Dirty still masks behind/ahead in `RepoStatus` (single-variant enum) — visible as `*` in status, counted under skipped in pull.

---

## 2026-09-16: Phase 0 — machine-readable outputs

### JSON contracts + exit codes + dirty counts (ROADMAP Phase 0)
- [x] `--json` (`json_version: 1`) for `status`, `list`, `ps`, `audit`; shapes in `src/cli/json.rs`, spec in ROADMAP.md
- [x] `spark audit` exits 1 on findings (CI gate); `spark status --exit-code` opt-in for agents/CI
- [x] `RepoStatus::Dirty { ahead, behind }` — dirty repos now show counts ("Dirty, 304 behind"); cache round-trip keeps legacy `dirty` compat
- [x] `Serialize` (snake_case) on scanner finding types (secrets, history, patterns, deps)
- [x] repo_manager tests moved to `tests.rs` (mod.rs back under the 500-LOC convention)
- [x] Verified live: JSON parses for all four commands; audit exit 1/0; dirty+behind counts on the real fleet

---

## 2026-09-16: Agent-first focus defined

### Strategy analysis → [ROADMAP.md](../ROADMAP.md)
- [x] Adoption audit: 0 stars / 14 npm downloads vs `trs` 840 downloads — the audience that travels is agents, the format that travels is JSON
- [x] Market check: single-repo TUIs (lazygit/gitui) and cleaners (mole) are served; local-machine DevOps for agents is nearly empty
- [x] Focus chosen: **agent-first local DevOps** — one CLI, JSON contracts, exit codes; TUI remains the human view
- [x] MCP server parked (CLI + skill covers in-house agents; revisit on third-party distribution)
- [x] `docs/dev/ROADMAP.md` written with Phase 0-2 specs and JSON schemas
- [x] TASK_TODO restructured: current focus (Phases 0-2) / backlog / parked

---

## 2026-09-16: parallel audit phases, lazy repo size, tilde-expanding config

### Perf + correctness
- [x] Audit phases 1-3 (secrets, git history, code patterns) run in parallel via `thread::scope`; deps phase overlaps on the main thread (keeps the tokio `block_in_place` context it needs)
- [x] `list_managed_repos_lite()` — skips the `.git` size walk (`size = 0`); CLI callers (list/search/cd/rm/status/pull/tags/ingest/doctor) use it, TUI keeps sizes for its table
- [x] Config `~/` paths expand on load (`scan_directories` + `repos_root`) — previously silently skipped as literal `~` dirs
- [x] Struct-level `#[serde(default)]` — partial/old config files merge with defaults instead of being discarded entirely
- [x] `expand_tilde` handles bare `~`; `config.example.toml` `max_scan_depth` 4 → 6 (matches the real default)
