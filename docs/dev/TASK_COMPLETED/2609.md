# SPARK — Completed Tasks: September 2026

## 2026-09-15: parallel repo status + pull overhaul

### spark status / spark pull — parallel fetch, no double-fetch, classified output
- [x] `check_statuses_parallel()` — bounded pool (`STATUS_CONCURRENCY = 8`) over `std::thread::scope` + mpsc; progress `[n/total]` to stderr
- [x] `spark status` — parallel check for uncached repos; cache saved in one batch write (`save_statuses_to_cache`) instead of N file rewrites
- [x] `spark pull` — same pool; `Behind` repos merge via new `merge_ff_only()` (`git merge --ff-only @{upstream}`) reusing the status fetch — no second network fetch per repo
- [x] `git fetch --quiet --prune` — stale remote-tracking refs auto-pruned (fixes "some local refs could not be updated" errors)
- [x] Fetch failures surfaced: `UpToDate` + failed fetch now reports `Error("fetch failed: …")` instead of a stale "Up to date"
- [x] Pull summary now separates buckets: pulled / up to date / skipped (Ahead, Dirty, Diverged — diverged no longer attempts a doomed `--ff-only`) / errors
- [x] Error output condensed to the `fatal:`/`error:` line + remediation hint (`spark rm` for dead remotes, `git refs migrate --ref-format=reftable` for case-conflicts, `git remote prune origin` for stale refs)
- [x] `list_managed_repos` reads remote/branch/last-commit via git2 — drops 3 `git` subprocess spawns per repo
- [x] `is_cache_valid` uses `saturating_sub` (no underflow on clock skew)
- [x] TUI `start_check_repo_statuses` — same bounded-pool pattern over tokio `spawn_blocking` workers; rows still update incrementally
- [x] Shared `select_repos()` tag/query filter for `status`/`pull` (was duplicated)

Notes: 145 repos checked in ~21s fresh (was sequential: minutes). Dirty still masks behind/ahead in `RepoStatus` (single-variant enum) — visible as `*` in status, counted under skipped in pull.
